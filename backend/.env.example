# MongoClone Backend Configuration
PORT=8080
DATA_DIR=data
DEFAULT_TARGET_URI=mongodb://user:password@host1,host2/target_db?authsource=admin
DEFAULT_TARGET_NAME=Target DB
# Centralized MongoDB Storage URI (stores profiles, clone jobs, schedules, and checkpoints in MongoDB)
PROFILES_DB_URI=
