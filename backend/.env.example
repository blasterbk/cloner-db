# MongoClone Backend Configuration
# NOTE: Connection profiles are now stored in browser localStorage.
# No external database is required to run this tool.

PORT=8080
DATA_DIR=data

# Optional authentication
AUTH_USERNAME=
AUTH_PASSWORD=

# Performance tuning
DEFAULT_BATCH_SIZE=5000
DEFAULT_PARALLEL_WORKERS=6
